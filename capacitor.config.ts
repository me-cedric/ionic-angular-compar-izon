import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-compar-izon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
